{
  "collections": [
    {
      "name": "users",
      "path": "users/{userId}",
      "description": "Stores user-specific app state JSON.",
      "fields": [
        {
          "name": "appState",
          "type": "string",
          "description": "The stringified JSON of the entire app state."
        },
        {
          "name": "updatedAt",
          "type": "timestamp",
          "description": "The last time the state was synced."
        }
      ]
    }
  ]
}
